on server
```bash
tailscale up --accept-dns=true
tailscale up --advertise-routes=192.168.0.123/32 --accept-routes
```
In tailscale:
enable sub routing and pass router from virtual network
sometimes restart is needed

